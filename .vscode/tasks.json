{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake: build (cj)",
            "type": "shell",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "args": [],
            "command": [
                "cls;",
                "cd ${workspaceFolder}/build;",
                "cmake --build . --config Debug --target all -j 16 --"
            ],
            "problemMatcher": []
        }
    ]
}